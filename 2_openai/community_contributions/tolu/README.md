# Education Policy Deep Research (OpenAI Agents SDK)

Multi-agent flow: planner → parallel web search → structured policy brief. Email omitted, only OPENAI_API_KEY needed.

## Setup
Use the repo’s existing virtualenv and dependencies (agents, openai, gradio, python-dotenv).  
Export secrets locally:
```bash
export OPENAI_API_KEY="sk-..."