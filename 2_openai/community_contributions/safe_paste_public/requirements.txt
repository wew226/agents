# Use the course repo environment when possible (see ../../README or repo root pyproject).
# Minimal pins if installing this folder standalone:
openai-agents>=0.0.17
python-dotenv>=1.0.0
pydantic>=2.0.0
gradio>=5.22.0
